type PostFeed {
  posts: [posts!]!
  nextCursor: DateTime
}

type StoryFeed {
  groups: [StoryGroup!]!
}

type StoryGroup {
  user: users!
  stories: [stories!]!
}

extend type Query {
  postFeed(cursor: DateTime, limit: Int = 50): PostFeed!
  storyFeed: StoryFeed!
}